# Frontend SIMCO

## Configuración de API

El frontend consume el backend usando `VITE_API_URL`.

Crea un `.env` (o usa `.env.example`) con:

```
VITE_API_URL=http://localhost:4000/api
```

En servidor, cambia ese valor por tu URL real.
